import BundleClient from './bundle-client';

export const metadata = {
  title: 'Numerology Bundle • Spirit Numeral',
  description: 'Get the full numerology bundle: Blueprint, Relationship, and Wealth reports.',
};

export default function BundlePage() {
  return <BundleClient />;
}
